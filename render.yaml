services:
  - type: web
    name: coconut-super-kid-bank
    runtime: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: NODE_VERSION
        value: "22"
